import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com", "cataas.com", "i.pravatar.cc", "via.placeholder.com"],
  },
};

export default nextConfig;
